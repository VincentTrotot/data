{% extends 'base.html.twig' %}

{% block title %}Edit Plein{% endblock %}

{% block body %}
    <h1>Edit Plein</h1>

    {{ include('plein/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_plein_index') }}">back to list</a>

    {% if is_granted('ROLE_ADMIN') %}{{ include('plein/_delete_form.html.twig') }}{% endif %}
{% endblock %}
