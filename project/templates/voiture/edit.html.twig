{% extends 'base.html.twig' %}

{% block title %}Modifier la voiture{% endblock %}

{% block body %}
    <h1>Modifier la voiture</h1>

    {{ include('voiture/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-default btn-sm" role="buttun" href="{{ path('app_voiture_index') }}">Retour à la liste</a>

    {% if is_granted('ROLE_ADMIN') %}{{ include('voiture/_delete_form.html.twig') }}{% endif %}
{% endblock %}
